// Track TBD (jazz / NYC-vibe direction) — drop the file at public/audio/theme.mp4
// once it's picked. Everything else (unlock-on-tap, silent-switch bypass via
// <video>, mute toggle) already works against this path.
export const MUSIC_SRC = "/audio/theme.mp4";
